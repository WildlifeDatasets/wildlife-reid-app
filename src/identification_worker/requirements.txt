celery[redis]==5.2.7

# database
psycopg2-binary~=2.9.5
SQLAlchemy~=1.4.44


# timm==0.9.7  # works fine, just in taxonworker is 0.9.12
timm==0.9.12
pycocotools==2.0.7
# pandas==2.0.2
pandas==1.5.3 # because of glue
kornia==0.7.1

# because of glue
numpy==1.26.4

# FGVC library
# removed till the piva-ai.com is down
# --index-url https://pypi.piva-ai.com/simple/
# fgvc==1.5.1.dev

# Wildlife-tools library
# wildlife-tools==0.0.5

wandb

# opencv-python==4.8.0.74

# for OCR
pytesseract==0.3

# for exiftool
pyexiftool==0.5.*
scikit-image==0.24.*

# timezones
pytz==2025.2
tzdata==2025.2

# we need update because of .webp support
pillow==12.0.0
